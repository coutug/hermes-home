# Home profile overrides
#
# This file is intentionally free of secrets. Keep API keys in an untracked env file.

model:
  default: null
  provider: null
  base_url: null
  api_key: null

agent:
  max_turns: 90
  tool_use_enforcement: true

display:
  tool_progress: true
  show_reasoning: false
  show_cost: false
