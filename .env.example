# Example environment variables for hermes-home
# Copy to .env locally if you need them, but do not commit secrets.
# Set HERMES_HOME to make Hermes read configs directly from this repo.

HERMES_HOME=/opt/data/hermes-home
HERMES_HOME_GH_TOKEN=
